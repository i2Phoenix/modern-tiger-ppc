PACKAGE_NAME=security-shim
PACKAGE_VERSION=$SECURITY_SHIM_PACKAGE_VERSION
BUNDLE_ID=com.modern-ppc.security-shim
TITLE='SecureTransport compatibility shim'
DESCRIPTION='Security.framework wrapper that maps the Tiger SecureTransport API to OpenSSL while forwarding non-TLS Security APIs to a local backup.'
REQUIRED=false
CHOICE_SELECTED=true
CHOICE_ENABLED=true
CHOICE_VISIBLE=true
RESTART_ACTION=RequiredRestart
