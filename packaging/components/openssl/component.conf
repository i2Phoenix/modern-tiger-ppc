PACKAGE_NAME=openssl
PACKAGE_VERSION=$OPENSSL_VERSION
BUNDLE_ID=com.modern-ppc.openssl
TITLE='OpenSSL 3.5.6'
DESCRIPTION='Modern OpenSSL with TLS 1.2 and TLS 1.3 support. Required by the networking stack and Security shim.'
REQUIRED=true
CHOICE_SELECTED=true
CHOICE_ENABLED=false
CHOICE_VISIBLE=true
